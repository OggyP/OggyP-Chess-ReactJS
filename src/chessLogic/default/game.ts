class DefaultGame {
    variation: string
    mode: string
    constructor(gameVariation: string, gameMode: string) {
        this.variation = gameVariation
        this.mode = gameMode
    }
}

export default DefaultGame